//Exercício: pedir 2 números ao usuário e exibir sua soma

const num1 = +(window.prompt('Digite um número: '));

const num2 = +(window.prompt('Digite outro número'));

alert (`O valor da soma é: ${num1 + num2}.`);